# build output (Vercel regenerates via `npm run build`)
dist/

# dependencies
node_modules/

# logs / temp
*.log
scripts/_*.log
scripts/_*

# old monolith + backups (superseded by the data-driven build)
index.html
index.html.bak
index.html.cutout.bak
index.html.it3.bak
index.html.precut.bak
json.json

# local editor/agent config (preview server, etc.)
.claude/

# OS
.DS_Store
Thumbs.db

# agent skills installed locally (higgsfield etc.)
.agents/
skills-lock.json

# supplier feed working copies (photos are processed into assets/img/products)
scripts/_feed_*.xml

# research reports from scripts/enrich_specs.mjs (review, then --apply)
scratch/
